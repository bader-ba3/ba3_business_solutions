import 'package:flutter/material.dart';

class DeleteInventoryView extends StatelessWidget {
  const DeleteInventoryView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
