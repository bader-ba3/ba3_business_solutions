import 'package:flutter/material.dart';

class DeleteInventoryView extends StatelessWidget {
  const DeleteInventoryView({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
